import LoginSignupPage from '@/components/Login/login';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginSignupPage />
        </div>
    );
};

export default page;
