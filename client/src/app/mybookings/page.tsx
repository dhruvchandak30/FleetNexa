import MyBookings from '@/components/Driver/MyBookings';
import React from 'react';

const page = () => {
    return (
        <div>
            <MyBookings />
        </div>
    );
};

export default page;
