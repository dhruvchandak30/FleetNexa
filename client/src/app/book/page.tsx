import BookingPage from '@/components/Bookings/BookingPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <BookingPage />
        </div>
    );
};

export default page;
